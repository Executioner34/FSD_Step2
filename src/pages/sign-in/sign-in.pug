include ../../components/header/header
include ../../components/footer/footer
include ../../components/signin-card/signin-card

doctype html
html(lang="ru")
	title sign-in
	body
		+header({isLogin: true})
		main(class='main' role='main')
			.main__image
				.main__sign-in-card
					+signIn({})
		+footer({})